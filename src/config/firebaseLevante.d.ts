export {}; // Add this line to make it a module

// Placeholder type, adjust based on actual export
declare module './firebaseLevante' {
  const config: any; // Replace 'any' with the actual config type if known
  export default config;
} 